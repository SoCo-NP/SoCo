# 🚀 Java Socket Programming: SoCo(Socket Coding)

**한성대학교 네트워크 프로그래밍 - 소켓 통신 프로젝트**

**SoCo**는 교수자가 학생들에게 실시간으로 코딩을 가르칠 수 있도록 설계된 **교육용 실시간 코딩 교육 IDE**입니다. 
이 프로젝트는 **교수(Professor)**와 **학생(Student)**의 역할을 명확히 구분하여 효율적인 원격 교육 환경을 제공하는 것을 목표로 합니다.
순수 자바 소켓(Java Socket)과 Swing을 사용하여 구현되었으며, 유지보수성과 확장성을 고려해 **계층형 아키텍처**와 **MVC 패턴**을 적용했습니다.

---

## 🏗 아키텍처 (Architecture)

이 프로젝트는 유지보수성과 확장성을 위해 **4계층 아키텍처**를 채택했습니다. 특히 **UI와 네트워크 로직의 완전한 분리**에 중점을 두었습니다.

### 🏛 전체 구조도

```mermaid
graph TD
    subgraph Client Application
        UI[UI Layer (ide.ui)] <-->|Interface| App[App Layer (ide.app)]
        App <-->|Calls| Domain[Domain Layer (ide.domain)]
        App <-->|Callbacks| Net[Infra Layer (ide.net)]
    end
    
    Net <-->|Socket/TCP| Server[Server Application (ide.server)]
```

### 📂 패키지별 상세 역할

| 계층 (Layer) | 패키지 | 역할 | 비고 |
| :--- | :--- | :--- | :--- |
| **Presentation** | `ide.ui` | **View**. 화면을 그리고 사용자 입력을 받습니다. | **소켓 코드 없음.** `CollabActions` 인터페이스를 통해 컨트롤러와 통신. |
| **Application** | `ide.app` | **Controller**. UI와 네트워크 사이를 중율합니다. | 메인 컨트롤러인 `CollabIDE`가 위치함. |
| **Domain** | `ide.domain` | **Model**. 순수 비즈니스 로직과 데이터 객체. | `User`, `Role` 등 POJO 객체. |
| **Infrastructure** | `ide.net` | **Adapter**. 실제 소켓 통신을 수행합니다. | `CollabClient`, `Protocol`. |
| **Server** | `ide.server` | **Server**. 클라이언트와 별도로 동작하는 중계 서버. | 멀티스레드 기반 브로드캐스팅. |

---

## ✨ 핵심 기능 (Features)

1.  **실시간 교육 및 협업 (Real-time Teaching)**
    *   **교수(Professor)**가 작성하는 코드가 모든 **학생(Student)**의 화면에 실시간으로 동기화됩니다.
    *   반대로 학생이 질문을 위해 코드를 수정하면 교수자에게도 즉시 반영됩니다.

2.  **원격 교육 도구 (Remote Teaching Tools)**
    *   **레이저 포인터**: 교수자가 특정 코드 라인을 가리키면 학생들의 화면에도 붉은 점이 표시되어 시선을 집중시킬 수 있습니다.
    *   **Follow Me (화면 동기화)**: 교수자가 스크롤을 이동하면 학생들의 화면도 자동으로 해당 위치로 이동합니다.

3.  **역할 기반 권한 관리 (Role-Based Access Control)**
    *   **Professor**: 프로젝트 관리(파일 생성/삭제), 레이저 포인터, 강제 화면 동기화 등 수업 진행 권한 보유.
    *   **Student**: 코드 작성 및 열람 가능.
    *   **Guest**: 제한된 권한.

4.  **프로젝트 동기화**
    *   트리 구조의 파일 탐색기를 통해 수업 자료(소스 코드)를 체계적으로 관리합니다.
    *   파일 생성, 삭제, 이름 변경 등의 변경 사항이 모든 참여자에게 즉시 전파됩니다.

---

## 🛠 기술 스택 (Tech Stack)

*   **Language**: Java 17+
*   **GUI Library**: Java Swing (Custom UI Components)
*   **Networking**: `java.net.Socket`, `java.net.ServerSocket` (Blocking I/O)
*   **Architecture**: Layered Architecture + Client-side MVC

---

## 🚀 실행 방법 (Getting Started)

프로젝트 루트에서 제공된 쉘 스크립트를 사용하여 간편하게 실행할 수 있습니다.

### 1. 서버 실행 (Server)
먼저 중계 서버를 실행해야 합니다.
```bash
./run-server.sh
```
*   기본 포트: 6000

### 2. 클라이언트 실행 (Client)
새로운 터미널 창을 열고 클라이언트를 실행합니다. 여러 개를 실행하여 테스트할 수 있습니다.
```bash
./run-client.sh
```

---

## 📁 프로젝트 폴더 구조

```text
.
├── ide/
│   ├── app/          # [Controller] 애플리케이션 로직 (CollabIDE.java)
│   ├── domain/       # [Model] 순수 도메인 객체 (User.java, Role.java)
│   ├── net/          # [Infra] 네트워크 클라이언트 및 프로토콜 (CollabClient.java, Protocol.java)
│   ├── server/       # [Server] 서버 애플리케이션 (CollabServer.java)
│   └── ui/           # [View] Swing UI 컴포넌트 (TabManager.java, FileTreeManager.java...)
├── run-client.sh     # 클라이언트 실행 스크립트
├── run-server.sh     # 서버 실행 스크립트
└── README.md         # 프로젝트 설명서
```

---

## 📝 개발자 노트 (Developer Notes)

이 프로젝트는 **"관심사의 분리(Separation of Concerns)"**를 최우선으로 리팩토링되었습니다.
네트워크 프로그래밍 실습 시, `ide.net` 패키지를 제외한 나머지 코드는 네트워크 구현 내용을 전혀 모르도록 격리되어 있어, 각 모듈을 독립적으로 학습하고 테스트하기 좋습니다.
